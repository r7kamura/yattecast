{
  "scripts": {
    "build": "katatema build",
    "deploy": "yarn run build && gh-pages --dist docs",
    "serve": "katatema serve"
  },
  "dependencies": {
    "katatema": "^0.1.2"
  },
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0"
  }
}
